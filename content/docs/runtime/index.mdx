---
title: Runtime model
description: The small set of objects that make up a running PhreshOS System.
icon: Cpu
---

The Program is the domain root. Everything live belongs to this hierarchy:

```text
Program
└── Process
    ├── ServerEndpoint
    └── ClientEndpoint
        ├── traffic
        └── window
```

`Program`, `Process`, `Endpoint`, `ServerEndpoint`, and `ClientEndpoint` are
shared runtime classes. `Endpoint` is the common base of `ServerEndpoint` and
`ClientEndpoint`; it is not an
additional ownership level.

<Cards>
  <Card title="Program" href="/runtime/programs">
    Stable application identity, installation, and resources.
  </Card>
  <Card title="Process" href="/runtime/processes">
    A live execution aggregate with immutable launch intent.
  </Card>
  <Card title="Endpoints" href="/runtime/endpoints">
    Host-side Server Endpoint and desktop Client Endpoint execution.
  </Card>
  <Card title="Communication" href="/runtime/communication">
    Context, exact destinations, questions, and subscriptions.
  </Card>
  <Card title="Services" href="/runtime/services">
    Stable addresses for live Endpoint capabilities.
  </Card>
</Cards>

## One model in every SDK

SDKs adapt access and authority, but they do not redefine the domain. Repeated
lookup of one live object within an SDK context returns its canonical handle,
so reference equality and `instanceof` remain meaningful.
