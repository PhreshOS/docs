---
title: Endpoints
description: The permanent Server and Client execution addresses inside a Process.
icon: Unplug
---

An Endpoint is one execution side of a Process. Every Process owns a
permanent Server Endpoint handle and a permanent Client Endpoint handle. A
handle remains the same while its live incarnation starts, stops, and starts
again.

Identity belongs to the Process structure. `exists()` describes whether code
is currently running at that address. `endpoint.lifecycle` publishes `start`
and `stop` whenever that incarnation changes.

## Shared behavior

Both Endpoint kinds can publish application events, observe messages, inspect
their outgoing traffic, find their owning Process, and report whether their
current incarnation is a Service.

```ts
const client = process.client

client.lifecycle.subscribe("start", () => console.log("Client started"))

await client.start()
await client.waitReady()
```

`waitReady()` waits for an incarnation that can be used. A Client is ready
when it exists. A Server must also announce readiness. The default deadline
is ten seconds. Temporary absence remains waitable while the owning Process
exists.

`start()` ensures that an incarnation is live. If one already exists, the
call resolves without replacing it or publishing another lifecycle event;
launch values configure an incarnation only when one must be created.
`stop()` resolves when the incarnation is already absent. Both operations
reject when the Process handle is no longer valid. Starting rejects when that
Endpoint kind was not declared. Stopping rejects if it would remove the
Process's final live Endpoint. Stopping never exits the Process.

## Server Endpoint

The Server Endpoint runs host-side code and answers questions.
`ask(event, payload?)` sends a question to the current Server incarnation.
`timeout(milliseconds)` selects another deadline.

```ts
const health = await process.server.ask("health.read")
```

A Server declaration chooses one execution mechanism: a supervised operating
system command, an ECMAScript module loaded in a Node.js Worker, or a bundled
module loaded in a capability-contained Sandbox. These mechanisms have the
same Server SDK and Endpoint contract. They differ in the host authority
available outside the System API. See [Security](/system/security).

`server.start({ service: true })` may override the Service role for that new
incarnation.

## Client Endpoint

The Client Endpoint runs in a Desktop iframe and owns a permanent `window`
handle. Its launch can choose the Service role and the initial title, header
state, size, position, layer, minimized state, and maximized state.

```ts
await process.client.start({
  title: "Inspector",
  layer: "over",
  size: { width: 520, height: 420 },
})
```

The iframe loads `/program/<assetId>/assets/`. In production, that address
serves installed Client files. During development, the System proxies the
declared Client development server through the same address, including its
WebSocket traffic.

See [Desktop](/system/desktop) for Windows.

## Traffic

`endpoint.traffic` observes communication originating from that Endpoint.
Publications appear as `{ to, payload }`. Questions appear through
`subscribeAsks()` and `asks()`. A Server's traffic can additionally observe
answers through `subscribeAnswers()` and `answers()`. An identity outside the
observer's authority is `null`.
