---
title: What is PhreshOS?
description: The System, the web desktop, and the model they share.
icon: CircleQuestionMark
---

PhreshOS is an open-source, server-authoritative operating system for running and
managing web-based programs in a unified web desktop environment, with native
agent access through shared APIs.

## The model

One owner runs the System on a machine they control. The System keeps the
authoritative state for installed software, live execution, communication,
persistence, authentication, and access to host capabilities.

The browser is the machine's display, not its authority. The server owns the
truth about Programs, Processes, authentication, persistence, and access to host
resources. A Desktop connection represents that truth and provides the local
pointer, keyboard, measurements, iframes, and temporary visual state needed to
interact with it.

<img
  src="/mental-models/system.png"
  alt="The PhreshOS System, containing the Server runtime and Desktop, within the host operating system"
  className="mental-model"
/>

## One unit of composition

Applications, background services, tools, and agents are Programs. Every
Program enters the same Program, Process, Endpoint, and Service model. An
official Program does not receive a separate architectural path.

Client, Server, and Node SDKs preserve the same domain contracts. Their
environments change implementation and authority, not the meaning of a
Program, Process, Endpoint, or Service.

## Goal

PhreshOS aims to be the self-hosted application operating system where web
applications, services, and agents operate as one coherent, interactive system.
It provides managed execution, bidirectional communication, and system-wide
identity and authorization, allowing each application to focus on its domain
instead of rebuilding infrastructure.

<Cards>
  <Card
    title="Programs and Processes"
    description="See how installed software becomes live execution."
    href="/runtime/programs"
  />
  <Card
    title="Client SDK"
    description="Access the System from a Desktop Client Endpoint."
    href="/sdks/client"
  />
  <Card
    title="Server SDK"
    description="Access the System from a Server Endpoint."
    href="/sdks/server"
  />
  <Card
    title="Desktop"
    description="Understand authoritative Windows and their local representations."
    href="/system/desktop"
  />
</Cards>

<Callout title="Runs on a host operating system">
  PhreshOS runs as a per-user service on macOS, Linux, or Windows. It does not
  replace the host kernel.
</Callout>
