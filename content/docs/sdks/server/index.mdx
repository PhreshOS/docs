---
title: Overview
description: Use PhreshOS from the Server Endpoint currently executing on the host.
---

`@phreshos/server` is the adapter for a Program's Server Endpoint. It exposes
the common Core `system` contract and the current Server `context`.

<Tabs items={["npm", "pnpm", "Bun", "Yarn Classic"]} groupId="package-manager" persist>
  <Tab value="npm">
    ```sh
    npm install @phreshos/server
    ```
  </Tab>
  <Tab value="pnpm">
    ```sh
    pnpm add @phreshos/server
    ```
  </Tab>
  <Tab value="Bun">
    ```sh
    bun add @phreshos/server
    ```
  </Tab>
  <Tab value="Yarn Classic">
    ```sh
    yarn add @phreshos/server
    ```
  </Tab>
</Tabs>

`@phreshos/core` is a peer dependency.

```ts
import { context, system } from "@phreshos/server"

context.answer("counter.read", async () => ({ value: 1 }))

const program = await context.program()
const appearance = await system.appearance.snapshot()
```

There is no `desktop` root. A Server is not a Desktop representation. It can
read and change the paired Client's authoritative Window through
`context.client.window`.

<Cards>
  <Card title="Context" description="The current execution, paired Client, and answers." href="/sdks/server/context" />
  <Card title="System access" description="Shared System capabilities from a Server Endpoint." href="/sdks/server/system-access" />
  <Card title="Permissions" description="Use the Program authority shared by both Endpoint kinds." href="/sdks/server/permissions" />
  <Card title="Execution" description="Run the Server through Command, Worker, or Sandbox." href="/sdks/server/execution" />
</Cards>
