---
title: Overview
description: Use PhreshOS from the Server Endpoint currently executing.
---

`@phreshos/server` implements PhreshOS contracts for a Program's current Server
Endpoint incarnation. It exposes the execution `context` and the
permission-constrained `system` root.

## Install

<Tabs items={["npm", "pnpm", "Bun", "Yarn Classic"]} groupId="package-manager" persist>
  <Tab value="npm">
    ```sh
    npm install @phreshos/server
    ```
  </Tab>
  <Tab value="pnpm">
    ```sh
    pnpm add @phreshos/server
    ```
  </Tab>
  <Tab value="Bun">
    ```sh
    bun add @phreshos/server
    ```
  </Tab>
  <Tab value="Yarn Classic">
    ```sh
    yarn add @phreshos/server
    ```
  </Tab>
</Tabs>

<Callout title="Peer dependencies">
  Server requires `@phreshos/core`.
</Callout>

```ts
import { context, system } from "@phreshos/server"
import { Program, Process } from "@phreshos/core"
```

Server exports only the roots it owns. Shared domain classes and types remain
Core imports, and handles returned by Server inherit those classes.

## One Server contract

`context` identifies the current Server, receives its messages, exposes its
paired Client, and registers answers. `system` provides the same complete
contract used by Client and Node. Every System operation uses the executing
Program's permissions, regardless of how the Server itself is hosted.

The execution environment is a separate choice. A command or Worker may use
host capabilities directly, while a Sandbox can leave its JavaScript runtime
only through the permission-constrained System contract. Application imports
remain the same in all three environments.

The Context begins and ends with one Server incarnation. A future incarnation
receives a new Context even though the permanent Server Endpoint handle remains
the same.

<Cards>
  <Card title="Context" description="Use execution identity, paired Client communication, and answerers." href="/sdks/server/context" />
  <Card title="System access" description="Use Program-authorized System capabilities across the Server boundary." href="/sdks/server/system-access" />
  <Card title="Execution environments" description="Choose command, Worker, or Sandbox execution without changing the Server contract." href="/sdks/server/execution-environments" />
</Cards>
