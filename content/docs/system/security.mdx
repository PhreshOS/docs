---
title: Security
description: One owner, structural Client isolation, and explicit permissions.
icon: ShieldCheck
---

Security begins with one System owner and structural authority boundaries.
Initial setup registers the owner and closes registration; later Desktop
sessions authenticate as that owner.

The System provides no independent recovery authority above the owner's chosen
authentication method.

## Client authority

A Client can access its own Program, that Program's Processes and resources,
Services belonging to that Program, read-only Appearance, Desktop surface and
preferences, and its Context capabilities. It cannot enlarge authority by
fabricating an identity or message. An inaccessible handle behaves like a
nonexistent one; a hidden sender or destination is represented as `null`.

## Permissions

Permissions come from the closed Core catalog. A Program cannot invent a
permission name or a value outside that permission's value domain.

Immutable initial grants are declared under `client.permissions`. Stored user
grants belong to the Program, while one owner decision may grant access only to
the current Process. The effective permission combines those three sources;
`all` overrides every narrower grant.

The current catalog is:

| Permission | Values | Default | Authority |
| --- | --- | --- | --- |
| `services` | Program identities | `[]` | Services belonging to every Program, or only the listed Programs |
| `programs` | Program identities | `[]` | Every Program, or only the listed Programs, including their Processes, Endpoints, Windows, resources, and Services |
| `appearance` | None | `[]` | Update System Appearance |
| `desktopPreferences` | None | `[]` | Update Desktop preferences |
| `all` | None | `[]` | Every available permission and the final unrestricted Client capabilities |

For `services` and `programs`, an empty list means every Program; a non-empty
list is an unordered set of selected Program identities. A `programs` grant
also grants Services belonging to the same Programs. Creating or force-creating
a Program and forking a Program require the unrestricted `programs: []` grant.

Value-less permissions use an empty list only as their canonical present value.
For example:

```ts
permissions: {
  services: ["web-browser"],
  appearance: true,
}
```

`true` is assignment and request shorthand for the permission's default value.
It therefore selects every Program for `services` and `programs`, and the
canonical present value for value-less permissions.

When present, `all` grants every System capability, uniquely permits
`program.permissions`, and adds `allow-same-origin` to the Client iframe.
Activating or removing that iframe capability requires a reload. The other
permissions take effect without reloading.

`context.permissions.get()` returns the stored user value.
`context.permissions.request()` returns `{ permission, needReload }`; it returns
an existing sufficient grant without prompting. Otherwise, the owner may grant
the request for the current Process, store it for the Program, deny it, or leave
it unresolved. Requests wait for up to two minutes by default, and `timeout()`
selects another deadline.

| Context operation | Result |
| --- | --- |
| `get(name)` | Stored `string[]`, `false`, or `null` |
| `request(name, value?)` | Resolved `{ permission, needReload }` |
| `timeout(milliseconds).request(...)` | The same request with another deadline |

`program.permissions` is the authoritative stored-grant capability:

| Program operation | Result |
| --- | --- |
| `get(name)` | Stored canonical value |
| `all()` | Stored values indexed by permission name |
| `set(name, value)` | Assigned value and derived reload requirement |
| `delete(name)` | `null` value and derived reload requirement |

`set()` accepts `true`, an allowed value set, or `false`; `null` belongs to
`delete()`. A permission declaration accepts only `true` or an allowed value
set. Arrays are canonicalized as unordered sets.

`needReload` is derived from the effective permission transition when the
request is resolved; it is not stored as permission state. `program.permissions`
can set or remove stored grants authoritatively and is itself available only
with `all`.

Asynchronous operations reject with `Execution is not permitted` when authority
is missing. Synchronous operations that cannot report failure remain silent.
